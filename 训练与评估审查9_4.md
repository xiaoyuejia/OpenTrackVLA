# AirGround-Coop V3 训练与评估审查（9 月 4 日整理版）

> 本文整理当前仓库中关于 AirGround-Coop V3 的数据、模型、训练、闭环评估、指标和运行参数审查结论。
>
> 主要代码依据：
>
> - `model_airground_coop_v3.py`
> - `candidate_matching.py`
> - `train_airground_coop_v3.py`
> - `train_airground_v3_common.py`
> - `eval_airground_coop_v3.py`
> - `eval_airground_v3_runtime.py`
> - `eval_unrealzoo_multi_agent.py`
> - `config/airground_cooperative_tracking_v3_yh_stable_joint.yaml`
> - `sh/eval_airground_coop_v3.sh`
> - `sh/eval_airground_coop_v3_24h_3gpu.sh`

---

## 1. 审查结论摘要

> **exp9_4 当前覆盖设置**：reference 实现、配置字段和测试继续保留，但
> `use_target_reference=false`。本轮训练、validation 以及由该 checkpoint 驱动的正式
> 评估均使用零 reference 且 `target_reference_valid=false`；以下 reference 小节描述的是
> 可恢复的保留实现，不代表 exp9_4 本轮启用。

当前 V3 已形成统一的 STT/DT/AT 联合训练与双智能体闭环评估流程：

1. STT、DT、AT 共用一个冻结的 Qwen3-0.6B、CandidateMatcher、8 路独立二分类 grounding 和 waypoint decoder。
2. 原始 instruction 保持权威：DT 保留外观描述，AT 保持模糊的初始目标规则，不注入外观答案。
3. 每个 agent 在一次 SELF Qwen forward 中完成 Top-K 候选校验和 SELF waypoint 解码；每个候选输出独立 sigmoid 概率，低于阈值时拒绝，不增加额外大模型调用。
4. SELF 完成后串行执行一次 COOP Qwen forward；COOP 使用双方视觉、位姿和 SELF 选中候选上下文。
5. STT/DT/AT 共享 episode 初始目标视觉 reference，但 reference 只作为辅助证据，不能覆盖 instruction 或绕过匹配阈值。
6. reference 代码仍支持训练静态 anchor 和评估动态确认/释放，但 exp9_4 本轮全部禁用，以直接检验 instruction/history/candidate grounding。
7. 评估为真实闭环：RGB → 在线感知/视觉编码 → 模型 → waypoint → 控制动作 → UnrealZoo 环境 → 下一帧。
8. GT bbox、GT mask 和 GT target pose 在正式 V3 评估中只用于指标，不作为模型 oracle 输入。
9. 新训练配置为 `train_temporal_stride=5`、`epochs=10`，相邻被采样 current row 间隔 0.5 s。
10. V3 评估协议已统一强制 drone 和 robotdog 的有效跟随距离为 `1.0–6.0 m`。

---

## 1.1 正式启动前 smoke 结果

已完成从头训练 smoke，并在正式训练前修复两个 grounding 数值问题：

1. 原始 cosine/temperature 残差会让独立 sigmoid 在初始化时直接饱和到约 `0.99997`，无法作为绝对匹配概率。现改为可训练 `cosine_gain`，并从 0 初始化；cosine 证据仍被保留，但只有在 balanced BCE 学到有用增益后才启用。
2. 两个视角均无有效正候选时，掩码后的极小 logit 经过 `softplus` 会先产生 `inf`，随后 `inf*0` 使 loss 变成 NaN。现先把无监督行替换为 0，再计算 `softplus`，并增加全无匹配候选回归测试。
3. 独立工具函数 `select_top_candidate()` 仍残留旧 margin gate。虽未被正式模型调用，但其语义与本轮协议不一致；现已改为只按最高 sigmoid 概率和置信度阈值接受，margin 仅返回用于诊断。

修复后的验证结果：

- 单 GPU、40 optimizer step smoke：40 行日志全部有限；全无监督的 step 26 正确得到 `loss=0`，不再产生 NaN。
- 双 GPU DDP、正式 `batch_per_gpu=48`、`grad_accum_steps=2` smoke：成功完成 20 个 optimizer step（约 3840 个样本），无 DDP unused-parameter、NCCL、NaN 或 OOM 错误。
- DDP grounding 概率不再饱和：第 1 step 的正候选/最大负候选概率约为 `0.522/0.524`；第 20 step 约为 `0.537/0.484`，Top-1 匹配准确率由 `0.558` 升至 `0.926`。这里只说明梯度和数值链路有效，尚不足以标定正式阈值。
- 相关测试：`pytest -q tests` 为 `64 passed`。
- 全仓库裸跑 `pytest -q` 会额外收集内嵌 `habitat-lab` 测试，并因当前环境缺少可选依赖 `magnum` 在 collection 阶段停止；这不是 AirGround V3 测试失败。

仍需在正式训练开始后重点监控：

- 双卡 smoke 峰值显存约 `92.4 GiB/GPU`，当前 96 GiB 卡可运行，但余量有限；若正式任务发生 OOM，应优先将单卡 batch 从 48 降到 44，而不是改变模型逻辑。
- scratch 的 `grad_norm_preclip` 从第 1 step 的约 `1.45e4` 下降到第 20 step 的约 `2.31e3`，20 step 全部触发 `clip=1.0`；同时总 loss 已从 `401.2` 降到 `32.8`。需要继续观察前 100–300 step，若梯度长期保持四位数且 grounding 不再改善，再调整 scratch warmup 或 loss 权重策略。
- `0.50` 阈值不能用几十个初始化 step 标定；应依据后续 validation 中正负概率分布再决定是否通过环境变量覆盖。

---

## 2. 任务与数据组成

### 2.1 联合训练任务

| 任务 | 训练 episode 数 | instruction 语义 |
|---|---:|---|
| STT | 5614 | 单目标持续跟踪规则 |
| DT | 1933 | 使用目标外观描述区分目标与干扰者 |
| AT | 1933 | 使用模糊的“初始目标身份”规则，不提供具体外观答案 |
| 合计 | 9480 | 三任务联合采样和联合优化 |

DT 与 AT 可以对应同一环境 episode，但语言规则不同。`task_type` 仅用于数据审计和统计，不切换模型分支，也不启用独立任务 loss。

### 2.2 训练数据位置

| 数据 | 路径 | 用途 |
|---|---|---|
| JSONL 标注 | `/data/yh/data/processed/train_jsonl/{stt,dt,at}` | instruction、轨迹、GT bbox、位姿等监督信息 |
| vision cache | `/data/yh/data/processed/vision_cache` | 离线 DINO+SigLIP 视觉 token |
| perception cache | `/data/yh/data/processed/perception_cache` | YOLO Top-K 候选、检测框和场景感知结果 |
| 联合训练 manifest | `/data/yh/data/manifests/train_joint.json` | 9480 个训练 episode 清单 |
| 验证 manifest | `/data/yh/data/manifests/val_joint.json` | 2400 个验证 episode 清单 |

训练通常不直接读取 JPG，而是读取 JSONL、vision cache 和 perception cache。评估则实时读取 UnrealZoo RGB，并在线执行 YOLO 和视觉编码。

### 2.3 核心 batch 形状

| 字段 | 形状 | 含义 |
|---|---|---|
| `coarse_tokens` | `(B,2,124,1536)` | 双 agent 的 31 帧历史粗粒度视觉 token |
| `fine_tokens` | `(B,2,64,1536)` | 双 agent 当前帧细粒度视觉 token |
| `candidate_feat` | `(B,2,8,6)` | 每个 agent 最多 8 个候选框特征 |
| `candidate_valid` | `(B,2,8)` | 候选有效掩码 |
| `waypoints` | `(B,2,8,3)` | 双 agent 的 8 个 `[x,y,yaw]` 监督点 |
| `agent_poses` | `(B,2,4)` | 双方位置和航向编码 |
| `target_reference` | 按 batch 拼接的视觉 token | episode 初始目标身份辅助证据 |

---

## 3. Instruction 与 reference 合同

### 3.1 Instruction 是权威目标规则

- `batch["instruction"]` 保留数据中的原始目标规则。
- instruction 只在 `Target rule` 中出现一次，避免旧实现重复拼接导致文本超长。
- STT、DT、AT 不进行任务专属 prompt 分支。
- DT 的外观描述不能被 reference 覆盖。
- AT 只说明保持初始目标身份，不能把 `target_description` 作为答案注入模型。

实测完整 SELF 文本长度：

| 任务 | token 数范围 |
|---|---:|
| STT | 约 91 |
| DT | 约 108–115 |
| AT | 约 92–97 |

均未超过 `text_max_length=128`。

### 3.2 训练 reference

1. 从 episode step 0 的有效 GT bbox 区域池化 fine visual token。
2. step 0 自身不提供 reference，后续 step 共享同一个静态 anchor。
3. 如果 step 0 的 `bbox_valid_mask=False`，整个 episode 的 reference 保持无效，不向后搜索其他帧，也不池化背景。
4. 使用 `train_target_reference_dropout_prob=0.20`，防止模型绕过 instruction。

### 3.3 评估 reference

评估不能直接使用 step 0 GT，因此采用动态确认：

- 连续 3 帧高置信且身份一致后建立 reference；
- 建立后冻结，避免逐帧更新造成漂移；
- 连续 12 帧拒绝后释放 reference，并允许重新定位；
- reference 不能强制接受低置信候选，也不能绕过候选匹配阈值。

---

## 4. 训练数据流

```text
JSONL + vision cache + perception cache
                │
                ▼
联合 manifest / sampler
                │
                ▼
dataset 读取当前帧、31 帧历史、Top-8 候选、位姿和 waypoint
                │
                ▼
构造静态 episode reference + 合成遮挡
                │
                ▼
collate 得到双 agent batch
                │
                ▼
SELF Qwen forward（两条 agent stream 沿 batch 维打包）
                │
                ├─ VERIFY hidden 与每个候选 hidden 显式匹配
                ├─ 8 个独立 sigmoid 匹配概率，按阈值接受最高项
                ├─ 选中 candidate context 注入 ACT hidden
                └─ 解码 SELF waypoint
                │
                ▼
COOP Qwen forward（双方视觉、位姿、相对位姿、候选上下文）
                │
                ├─ JEPA 恢复 receiver 缺失语义
                ├─ 4-mode cooperative waypoint decoder
                └─ mode classification / belief / uncertainty
                │
                ▼
路由输出 + 多项 loss
                │
                ▼
backward → grad accumulation → clip → optimizer → scheduler
                │
                ▼
定期保存 checkpoint
```

### 4.1 SELF 输入

单个 agent 的逻辑序列：

```text
[text | reference | history(155) | current(65) |
 candidate(8) | VERIFY | ACT]
```

两名 agent 的 SELF stream 沿 batch 维合并，因此只执行一次 SELF Qwen forward，但双方行内视觉信息相互隔离。

### 4.2 Candidate grounding

每个候选都有 Qwen 输出的 candidate hidden。`CandidateTextMatcher` 使用：

```text
[candidate, verify, candidate*verify, candidate-verify]
```

结合 MLP 与 cosine similarity 为每个候选产生独立 logit，再分别经过 sigmoid；8 个概率不要求和为 1。

训练标签规则：

- 在有效 Top-K 候选中选取与 GT bbox IoU 最大的候选作为唯一正样本；
- 其余有效候选作为负样本，正负 BCE 分组求均值后等权组合；
- 最大 IoU 未达到 `0.30` 的 view 不产生候选 BCE；
- 目标位于 rank 2..K 时仍然是有效目标，不再由 YOLO top-1 决定可见性；
- 推理选择 sigmoid 最高项，并使用单一 `0.50` confidence 阈值拒绝弱匹配；margin 仅记录为诊断指标。

选中候选上下文通过零初始化残差注入 ACT hidden：

```text
h_ACT_grounded = h_ACT + target_context_proj(z_target)
```

这样 grounding 会显式影响 waypoint，而不是只作为附属分类损失。

### 4.3 COOP 输入与串行关系

COOP 使用：

- 双方 SELF stream 表征；
- 双方选中的 candidate context；
- 双方绝对位姿；
- receiver 局部坐标系下的有向相对位姿；
- COOP_ACT 查询。

SELF 和 COOP 是两次串行 Qwen forward。COOP 依赖 SELF 的 `selected_candidate_context`，因此当前不能与 SELF 并行。当前实现无条件计算 COOP，最后再由路由选择采用 SELF、COOP 或 belief 输出。

### 4.4 主要训练损失

| 损失 | 作用 | 权重 |
|---|---|---:|
| SELF waypoint / `beta_nav` | 单 agent 局部轨迹学习 | 100.0 |
| cooperative waypoint | 协同恢复轨迹学习 | 100.0 |
| mode classification | 4-mode 轨迹模式选择 | 1.0 |
| candidate balanced BCE | Top-K 独立二分类，正负组等权 | 1.0 |
| JEPA | 缺失视觉语义恢复 | 1.0 |
| target belief | 目标信念预测 | 1.0 |
| uncertainty | 不确定性约束 | 0.1 |
| smoothness | 轨迹平滑 | 0.1 |
| kinematics | 运动学约束 | 0.1 |
| diversity | 多模态轨迹差异 | 0.1 |

兼容字段 `loss_candidate_bce` 已修复假 NaN：不再对 masked 极端 logits 求和后乘零。

---

## 5. 最新训练参数表

配置文件：`config/airground_cooperative_tracking_v3_yh_stable_joint.yaml`

### 5.1 数据与模型

| 参数 | 当前新值 | 作用 |
|---|---:|---|
| `train_temporal_stride` | **3** | 以 3 个时间相位轮换采样；3 个 epoch 可覆盖一轮全部时间位置 |
| `epochs` | **6** | 训练 6 轮，相当于完整时间相位覆盖两轮 |
| `candidate_top_k` | 8 | 每帧最多 8 个候选人物 |
| `history` | 31 | 历史帧长度 |
| `n_waypoints` | 8 | waypoint 数量 |
| `action_dims` | 3 | `[x,y,yaw]` |
| `image_size` | 384 | 视觉输入尺寸 |
| `vision_feat_dim` | 1536 | DINO+SigLIP 特征维度 |
| `target_match_iou_threshold` | 0.30 | 候选正标签 IoU 阈值 |
| `train_target_reference_dropout_prob` | 0.20 | reference dropout |
| drone/dog 合成遮挡概率 | 0.50 / 0.50 | 构造单边不可见协同样本 |
| `llm_name` | Qwen3-0.6B | 共享语言模型 |
| `freeze_llm` | true | 冻结 Qwen 参数 |
| `text_max_length` | 128 | 文本长度上限 |
| `num_modes` | 4 | cooperative 多模态轨迹数 |
| `detection_confidence_threshold` | 0.25 | 检测有效门槛 |
| `target_match_confidence_threshold` | 0.50 | 推理匹配置信门槛 |

### 5.2 优化与运行

| 参数 | 值 | 作用 |
|---|---:|---|
| `batch_size` | 48 / GPU | 单卡 mini-batch |
| GPU 数 | 2 | NCCL DDP |
| `grad_accum_steps` | 2 | 两次 micro-batch 更新一次 |
| 有效全局 batch | 192 | `48 × 2 × 2` |
| `lr` | 4e-5 | 峰值学习率 |
| scheduler | cosine | 余弦学习率下降 |
| `warmup_steps` | 300 | 学习率预热 |
| `min_lr` | 4e-6 | 最低学习率 |
| `weight_decay` | 0.01 | 权重衰减 |
| `grad_clip` | 1.0 | 梯度裁剪 |
| `mixed_precision` | true | 混合精度训练 |
| `num_workers` | 8 | DataLoader worker 数 |
| `seed` | 731 | 随机种子 |
| `save_every` | 2000 | 每 2000 optimizer steps 保存 |
| `save_every_epochs` | 1 | 每个 epoch 保存 final |
| `max_ckpts` | 0 | 不裁剪历史 checkpoint |

### 5.3 旧训练终止与 loss 难度审查

旧训练输出目录：

```text
output/airground_v3_stable_joint_sharedref_20260904
```

该进程已按要求在 step 11802 发送 SIGTERM 并完整退出，两张 GPU 显存已释放。最近完整 checkpoint 为：

```text
model_epoch004_step010688_final.pt
```

step 8017 的 loss 抬升已定位为 receiver corruption curriculum 在 epoch 4 从 easy stage 离散切换到 hard stage：SELF、候选匹配和 JEPA 没有发散，主要是 cooperative loss 因 recent/all masking 与 pose perturbation 突然启用而增大。完整分析见：

```text
output/airground_v3_stable_joint_sharedref_20260904/loss_audit_after_step8000.md
```

新训练已改为 `stride=5、epochs=10`，并使用 `progressive_linear_10stage_v1`：保留按 epoch 渐进式加难，但把旧 epoch 4 的一次大跳拆成 10 个小台阶。

---

## 6. 闭环评估数据流

### 6.1 入口关系

```text
eval_airground_coop_v3.py
        │
        ├─ 解析并补充 V3 协议参数
        ├─ 使用 AirGroundCoopV3Planner
        └─ 调用 eval_unrealzoo_multi_agent.main()
```

`eval_unrealzoo_multi_agent.py` 负责 episode 加载、UnrealZoo 环境创建、闭环 rollout、逐步记录和指标汇总。

### 6.2 评估数据读取

`load_recorded_target_trajectories()` 从原始 episode 或 `split_manifest.json` 读取：

- test episode ID；
- `env_id`；
- 目标人物世界坐标轨迹；
- instruction / task metadata；
- 可选干扰者回放信息。

固定 manifest 默认指向：

```text
/data/yh/data/manifests/eval_all_2400_recorded.json
```

训练读取离线视觉缓存，评估读取录制目标轨迹并在 UnrealZoo 中实时渲染 RGB。

### 6.3 每个 episode 的初始化

`setup_episode()` 执行：

1. 重置 UnrealZoo 环境；
2. 确定 target、drone、robotdog 的 actor ID；
3. 设置 actor 外观和初始位置；
4. 设置评估相机；
5. 初始化录制目标轨迹和干扰者；
6. `planner.reset()` 清理历史、reference、路由和控制器状态；
7. 配置确定性 UE 时钟。

### 6.4 每步闭环

```text
① 同批读取 drone/dog RGB、pose；按指标采样策略读取 GT mask
                         │
                         ▼
② planner.predict()
   在线 YOLO Top-K → DINO+SigLIP → 动态 reference
   → SELF grounding/waypoint → COOP → 路由
                         │
                         ▼
③ planner.waypoints_to_actions()
   inverse_fixed_dt 将 waypoint 转成动作
                         │
                         ▼
④ UnrealZoo step
   人按录制轨迹移动，drone/dog 执行模型动作
                         │
                         ▼
⑤ 读取 action 后位姿，累计距离、following、碰撞、IoU、延迟
                         │
                         └──────────→ 下一帧
```

模型输出会改变下一步环境观察，因此这是闭环评估，不是对固定标签做离线预测。

### 6.5 路由语义

- `SELF`：本 agent 可靠看到并确认目标；
- `COOPERATIVE`：依赖另一 agent 信息恢复目标；
- `BELIEF`：短期目标丢失时使用信念轨迹；
- `SEARCH`：双方长期不可见后由 runtime 执行受限搜索。

搜索属于 runtime 行为，不是额外模型或额外 Qwen forward。

---

## 7. 最新评估参数表

V3 严格评估协议位于 `eval_airground_v3_runtime.py`；24h 启动脚本也已同步更新。

| 参数 | 当前值 | 作用 |
|---|---:|---|
| drone 跟随范围 | **1.0–6.0 m** | drone 可见且 action 后距离在此范围，记为 following |
| robotdog 跟随范围 | **1.0–6.0 m** | dog 使用相同判定范围 |
| `human_collision_distance` | 0.5 m | action 后距离严格低于该值，记人体碰撞 |
| `dt` | 0.1 s | 确定性固定仿真步长 |
| `max_steps` | 100 | 默认 episode 最大步数 |
| `human_speed` | 0.5 m/s | 回放目标速度 |
| drone/dog 最大速度 | 2.5 / 2.5 m/s | 动作速度限制 |
| `waypoint_control_mode` | `inverse_fixed_dt` | waypoint 到执行动作的逆动力学控制 |
| `policy_inference_stride` | **3** | 每隔 3 个确定性环境步执行一次模型，中间回放未来 waypoint segment |
| `robotdog_waypoint_y_mode` | `v3_nonholonomic_projection` | 适配机器狗非完整运动约束 |
| `bbox_source` | `none` | 禁止 GT/recurrent bbox 输入模型 |
| `bbox_motion_min_confidence` | 0.25 | bbox 运动辅助控制置信门槛 |
| `bbox_motion_ema_alpha` | 0.20 | bbox 信号平滑 |
| `max_lost_steps` | 400 | 防止固定录制序列因短期丢失过早结束 |
| `min_success_steps` | 20 | 提前结束时成功所需最少步数 |
| action oracle | `none` | 禁止 oracle 替换模型动作 |
| heading oracle | 禁用 | 禁止朝向 oracle 辅助 |

### 7.1 两类 stride 不要混淆

- `train_temporal_stride=5`：每隔 5 个 recorded row 采一个 current-frame 训练样本，相邻样本时间为 `5×0.1=0.5 s`。
- `policy_inference_stride=5`：闭环评估每隔 5 个环境步执行一次模型，policy interval 同样为 0.5 s。

V3 严格协议在 `eval_airground_v3_runtime.py` 中统一默认 `policy_inference_stride=5`，24h 启动脚本也显式设置为 5：

```text
policy_inference_stride=5
policy_action_rollout=future_segment
skip_rgb_between_policy_steps=true
```

必须注意：0.5 s 是训练 current-row 采样间隔和评估模型调用间隔，不是相邻 waypoint/action 的 dt。JSONL 明确记录 `waypoint_dt_s=0.1`、`source_action_indices=[0,1,...]`，中间 4 个环境步也逐点执行 future segment，因此环境控制、waypoint 速度/yaw rate、feasible recovery 和 inverse-fixed-dt 仍使用 0.1 s。把这些量改成 0.5 s 会把速度缩小 5 倍。

---

## 8. 指标定义

### 8.1 Per-step following

每个 agent 的 following 条件为：

```text
GT 可见
AND
1.0 m <= action 后 agent-target XY 距离 <= 6.0 m
```

联合 following：

```text
drone_following AND robotdog_following
```

对应汇总：

```text
drone_following_rate   = drone following 步数 / total_step
robotdog_following_rate = dog following 步数 / total_step
joint_following_rate   = 双方同时 following 步数 / total_step
```

`joint_following_rate` 是诊断指标，不直接替代终态 success。

### 8.2 碰撞

主碰撞标志为：

```text
collision = UE physical collision OR human collision
```

其中人体碰撞是 episode 粘滞指标：任一 follower 在 action 后与目标的距离小于 `0.5 m`，该 episode 后续一直记录为已碰撞。

同时分别保存：

- `physical_collision`；
- `human_collision`；
- `drone_human_collision`；
- `robotdog_human_collision`。

### 8.3 Bbox 与可见性

```text
drone_bbox_iou_mean = 每步 drone 预测 bbox 与 GT bbox 的 IoU 均值
robotdog_bbox_iou_mean = 每步 dog 预测 bbox 与 GT bbox 的 IoU 均值
visible_accuracy = 双方每步可见性预测正确数 / (total_step × 2)
```

GT bbox 和 GT mask 用于这里的指标计算，而不是模型目标选择 oracle。

### 8.4 Episode success

完整 horizon 结束：

```text
final_joint_following = true
```

提前结束：

```text
final_joint_following = true
AND
双方最终距离均位于 1.0–6.0 m
```

最终 success 还要求：

- 没有碰撞；
- 状态不是 `Lost/Collision/PersistentFailure/Timeout`；
- 完整运行到 horizon，或至少达到 `min_success_steps`；
- 满足终态 following 条件。

### 8.5 延迟指标

| 指标 | 含义 |
|---|---|
| `model_latency_ms` | 纯模型 forward 平均耗时 |
| `model_fps` | 纯模型 forward FPS |
| `fps` | 包含 UE、渲染、感知和控制的 episode 端到端吞吐 |
| `snapshot_seconds_total` | 观测查询总耗时 |
| `vision_encoding_seconds_total` | 视觉编码总耗时 |
| `perception_seconds_total` | 在线感知总耗时 |
| `action_step_seconds_total` | 环境动作推进总耗时 |

---

## 9. 已完成的重要修复

### 9.1 Top-K 与可见性

- 修复 `effective_visible` 由 YOLO top-1 决定的问题；
- 当前只要 Top-K 任一候选匹配 GT，目标就可被视为有效；
- rank 2..K 目标能够获得 SELF waypoint 监督；
- 禁用 Top-K 场景下有害的旧 `synthetic_false_positive` 路径；
- 真实 DT/AT 的 Top-8 recall 约 99%，明显优于 YOLO top-1 的约 78%/82%。

### 9.2 独立二分类 grounding

- 使用一个 VERIFY hidden 显式校验每个候选；
- 每个候选输出独立 sigmoid 概率，不设置第九类；
- 使用正负组等权 balanced BCE；
- 推理使用单一 `0.50` confidence 阈值拒绝弱匹配，margin 仅用于诊断；
- 保留训练 hard-forward / soft-backward 选择；
- 将候选上下文显式注入 ACT hidden；
- reference 设计继续保留。

### 9.3 Reference

- STT/DT/AT 共用初始目标视觉 reference；
- 训练静态、评估动态；
- 无有效 step 0 bbox 时不误取背景；
- step 0 自身不使用 reference；
- 20% dropout 防止绕过 instruction；
- reference cache 上限提高到 16384，减少 shuffled joint training 中的首帧重复加载。

### 9.4 Prompt 与信息泄漏

- instruction 原文不压缩、不改写；
- instruction 只在 `Target rule` 中出现一次；
- verification prompt 明确 instruction 优先于 reference；
- AT 不注入 `target_description`；
- synthetic receiver 不再获得 clean candidate context，避免 COOP 信息泄漏。

### 9.5 稳定性和 checkpoint

- 修复兼容指标 `loss_candidate_bce` 的假 NaN；
- 每 2000 optimizer steps 保存；
- 每个 epoch 保存 final；
- `max_ckpts=0`，不删除历史 checkpoint；
- 相关候选匹配/reference 测试曾达到 `62 passed`；
- 本次评估协议参数修改后，`tests/test_eval_airground_coop_v3.py` 为 `23 passed`。

---

## 10. 当前风险与待办

1. **旧训练已终止**：下一步可直接启动 `exp9_4` 的 scratch、无 reference、`stride=5、epochs=10、progressive_linear_10stage_v1` 正式训练。
2. **需要真实闭环 eval**：训练 loss 正常不能代替 UnrealZoo 闭环结果。
3. 重点统计 STT/DT/AT 分任务指标：candidate binary/top-1 accuracy、最高正负候选概率、threshold false accept、reacquisition、waypoint EPE、SR/TR。
4. 专门测试“初始若干帧无目标，后续目标出现”的动态 reference 建立过程。
5. 验证双方不可见时 BELIEF hold 到 SEARCH 的实际切换。
6. 观察早期随机 grounding 是否污染 waypoint；只有真实 eval 显示必要时，才加入最小 teacher-forcing warm-up。
7. `sh/run_top8_joint_s5_e5.sh` 仍存在 `${ROOT}/repo` 路径问题，后续应修复并增加非空输出目录保护。
8. 评估的 `policy_inference_stride=5` 是 0.5 s policy interval；报告结果时必须明确，不能与逐帧推理（`stride=1`）结果混为一组。

---

## 11. 推荐的最终验收顺序

1. exp9_4 使用 scratch 初始化，不加载任何历史 AirGround checkpoint；Qwen3-0.6B 保持预训练且冻结，optimizer/scheduler 全新创建。
2. 使用新配置启动 `train_temporal_stride=5、epochs=10` 训练，输出到新的目录，避免覆盖旧实验。
3. 先跑少量 STT/DT/AT 闭环 smoke eval，验证 reference 始终无效、候选阈值拒绝和路由状态。
4. 再跑固定 manifest 的正式评估，drone/dog 跟随范围统一为 `1.0–6.0 m`。
5. 汇总：
   - SR、joint/drone/dog following rate；
   - collision rate；
   - bbox IoU、visible accuracy；
   - candidate/NULL 指标；
   - reacquisition；
   - model latency、端到端 FPS。
6. 将最终 checkpoint、命令行、git diff、评估 manifest 和结果目录一起归档，确保实验可复现。

---

## 12. exp9_4 训练评估流水线

已在 `exp9_4/` 落实下一批实验流水线：

```text
训练（stride=5, epochs=10）
→ DT 500（视频 + 独立 metrics.csv）
→ AT 500（视频 + 独立 metrics.csv）
→ STT 1400（视频 + 独立 metrics.csv）
→ 三任务 aggregate 指标汇总
```

主要入口和参数：

- `exp9_4/pipeline.yaml`：流水线路径、GPU、顺序、视频和评估参数；
- `exp9_4/config/train_s5_e10.yaml`：训练参数模板；
- `exp9_4/run_pipeline.sh`：可续跑的串行执行入口；
- `policy_inference_stride=5`，policy interval=0.5 s，`policy_action_rollout=future_segment`；
- drone/dog 跟随距离均为 `1.0–6.0 m`；
- 每个 episode 保存 drone、robotdog 和 global MP4，并带轨迹 overlay；
- 模型保存在 `exp9_4/models/`，三任务结果分别保存在 `exp9_4/results/{dt,at,stt}/`。

完整说明见 `exp9_4/README.md`。旧训练已停止，流水线代码、目录、十阶段线性渐进 curriculum、dry-run 和测试均已验证；尚未自动启动新一批长时间实验。

---

## 13. 一句话总结

AirGround-Coop V3 当前保留完整 reference 实现，但 exp9_4 显式禁用它；grounding 使用一个 VERIFY、Top-8 独立 sigmoid 和 balanced BCE，推理按 `0.50` 阈值接受最高候选。评估端使用无 GT oracle 的 UnrealZoo 闭环协议。最新流水线配置为训练 `stride=5、epochs=10`，评估 `policy_inference_stride=5`（policy interval 0.5 s）、双方跟随距离 `1–6 m`，并按 DT→AT→STT 顺序保存独立指标和视频。
